#include <iostream>

int main(int argc, char *argv[])
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);

    int input;

    std::cin >> input;

    std::cout << 1 << '\n';
    std::cout << 0 << '\n';

    return 0;
}